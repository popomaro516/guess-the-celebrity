---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout>
  <section class="home container" aria-labelledby="home-title">
    <div class="home-heading">
      <h1 id="home-title">何をしますか？</h1>
      <p>
        画像の一部を切り抜いて4択クイズを作れます。公開された問題には誰でも回答できます。
      </p>
    </div>

    <div class="action-grid">
      <a class="action-card action-card-create card" href="/create/">
        <div>
          <span class="action-label">ログインが必要</span>
          <h2>クイズを作る</h2>
          <p>画像を選び、見せる範囲、問題文、4つの選択肢を設定します。</p>
        </div>
        <span class="button button-primary">
          作成を始める <span aria-hidden="true">→</span>
        </span>
      </a>

      <a class="action-card card" href="/play/">
        <div>
          <span class="action-label">ログイン不要</span>
          <h2>クイズに答える</h2>
          <p>公開されている問題からランダムに出題します。</p>
        </div>
        <span class="button button-outline">
          問題を始める <span aria-hidden="true">→</span>
        </span>
      </a>
    </div>
  </section>
</BaseLayout>

<style>
  .home {
    display: flex;
    min-height: calc(100vh - 64px);
    justify-content: center;
    padding-block: 72px 96px;
    flex-direction: column;
  }

  .home-heading {
    max-width: 680px;
    margin-bottom: 40px;
  }

  .home-heading h1 {
    margin-bottom: 12px;
    font-family: var(--font-body);
    font-size: clamp(2.25rem, 5vw, 3.5rem);
    font-weight: 500;
    letter-spacing: -1.2px;
  }

  .home-heading p {
    max-width: 620px;
    margin: 0;
    color: var(--color-muted);
    font-size: 16px;
    line-height: 1.7;
  }

  .action-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 20px;
  }

  .action-card {
    display: flex;
    min-height: 330px;
    padding: 32px;
    justify-content: space-between;
    flex-direction: column;
    box-shadow: none;
    transition: border-color 160ms ease, box-shadow 160ms ease, transform 160ms ease;
  }

  .action-card:hover {
    transform: translateY(-2px);
    border-color: var(--color-hairline-strong);
    box-shadow: var(--shadow-card);
  }

  .action-card-create {
    border-color: var(--color-hairline-strong);
  }

  .action-label {
    display: inline-block;
    margin-bottom: 28px;
    padding: 5px 10px;
    border-radius: 9999px;
    background: var(--color-surface);
    color: var(--color-muted);
    font-size: 12px;
    font-weight: 600;
  }

  .action-card h2 {
    margin-bottom: 12px;
    font-family: var(--font-body);
    font-size: clamp(1.6rem, 3vw, 2.1rem);
    font-weight: 500;
    letter-spacing: -0.4px;
  }

  .action-card p {
    max-width: 420px;
    margin: 0;
    color: var(--color-muted);
    font-size: 15px;
  }

  .action-card .button {
    width: fit-content;
    margin-top: 32px;
  }

  @media (max-width: 767px) {
    .home {
      min-height: calc(100vh - 60px);
      padding-block: 48px 64px;
    }

    .home-heading {
      margin-bottom: 28px;
    }

    .action-grid {
      gap: 14px;
    }

    .action-card {
      min-height: 290px;
      padding: 24px;
    }
  }

  @media (max-width: 479px) {
    .home {
      justify-content: flex-start;
      padding-block: 40px 56px;
    }

    .home-heading h1 {
      font-size: 2rem;
    }

    .action-grid {
      grid-template-columns: 1fr;
    }

    .action-card {
      min-height: 260px;
    }
  }
</style>
