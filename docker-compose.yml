services:
  api:
    build:
      context: ./server/api
    env_file:
      - path: ./server/api/.env.local
        required: false
    environment:
      HTTP_ADDR: ":8080"
      BASE_URL: "http://localhost:8080"
      AUTH_DISABLED: "true"
    ports:
      - "8080:8080"
    volumes:
      - ./server/api:/app
      - ~/.aws:/root/.aws:ro
      - go-mod-cache:/go/pkg/mod

volumes:
  go-mod-cache:
